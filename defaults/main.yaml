---
solr:
  url:
  prefix:
    bin: /var/opt/solr/solr
    home: /var/opt/solr/home
    logs: /var/log/solr-ansible
  version: 10.0.0
  oauth2_proxy:
  overwrite_home: no
  cloud: no
  nginx:
    client_max_body_size: 1M
  heap:
